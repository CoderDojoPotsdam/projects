/*class*/class ZeichneSiebenHamster extends ZeichneZifferHamster {
    ZeichneSiebenHamster(int r, int s, int b, int k) {
        super(r, s, b, k);
    }

    // zeichnet die Ziffer 7 mit Koernern ins Territorium
    void zeichne() {  // ueberschriebene und implementierte Methode
        // Implementierung ist Ihre Aufgabe
    }
}