/*class*/class ZeichneAchtHamster extends ZeichneZifferHamster {
    ZeichneAchtHamster(int r, int s, int b, int k) {
        super(r, s, b, k);
    }

    // zeichnet die Ziffer 8 mit Koernern ins Territorium
    void zeichne() {  // ueberschriebene und implementierte Methode
        // Implementierung ist Ihre Aufgabe
    }
}