/*class*/class MogelHamster extends SpielHamster {
    MogelHamster(int r, int s, Wuerfel w) {
        super(r, s, w);
        // tauscht heimlich den korrekten gegen einen
        // manipulierten Wuerfel aus
        this.wuerfel = new ManipulierterWuerfel();
    }
}