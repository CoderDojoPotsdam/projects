/*imperative program*/void main() {
    if (vornFrei()) {  // bedingte Anweisung
        vor();
        linksUm();
    }
    
    if (kornDa() && vornFrei()) { // Alternativanweisung
        nimm();
        vor();
    } else {
        linksUm();
    }
}