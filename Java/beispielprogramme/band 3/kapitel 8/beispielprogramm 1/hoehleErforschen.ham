/*object-oriented program*//*
 Windows:
 CLASSPATH: .;Programme\beispielprogramme\band 3
 Linux, Solaris:
 CLASSPATH: .:Programme/beispielprogramme/band 3
*/
void main() {
    int ANZAHL = 5;
    for (int i = 0; i < ANZAHL; i++) {
        (new HoehlenHamster(Hamster.getStandardHamster()
                .getReihe(), Hamster.getStandardHamster()
                .getSpalte())).start();
    }
}
